table:
  name: users
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - email
        - id
      filter:
        id:
          _eq: X-Hasura-User-Id
      limit: 1
      query_root_fields:
        - select_by_pk
      subscription_root_fields: []
